import React from "react";
import TextField from "../TextField";

function AddFreind() {
  return (
    <div>
      <TextField placeholder="Add freind name and press enter..." />
    </div>
  );
}

export default AddFreind;
